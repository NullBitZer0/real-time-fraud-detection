# CatBoost model config (default for the Sparkov pipeline)

model_name:     catboost
iterations:     500
depth:          8
learning_rate:  0.05
eval_metric:    PRAUC
early_stopping: 50
random_seed:    42
task_type:      CPU
