# Add patterns of files dvc should ignore, which could improve
# the performance. Learn more at
# https://dvc.org/doc/user-guide/dvcignore

# Feast runtime-generated files — regenerated by `feast apply` / `feast materialize`
# Never let DVC own these; they are not data, they are application state
feast/feature_repo/data/
feast/feature_repo/registry/
feast/feature_repo/online_store/
